{
  "name": "text-share",
  "version": "1.0.0",
  "description": "Text file sharing system based on Cloudflare Pages",
  "main": "functions/_worker.js",
  "scripts": {
    "dev": "wrangler pages dev functions",
    "deploy": "wrangler pages deploy functions"
  },
  "dependencies": {
    "wrangler": "^3.0.0"
  }
}
